import React from "react";
import { Formik, Form } from "formik";

const Register = () => {
    return (
        <>
            <Formik>
                {(formik) => {
                    return <Form>
                    
                    </Form>;
                }}
            </Formik>
        </>
    );
};

export default Register;
